<!DOCTYPE html>
<html>
<head>
  <title>Todo App</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application"%>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="row-fluid">
  <div class="span10 offset1">
    <div class="center hero-unit">
	  <h1>ToDo<h1>
      <h2>Welcome to the tutorial's ToDo application</h2>
      <%= link_to "New task", new_task_path, class: "btn btn-large btn-primary", remote: true %>
      <div class="text-center">
  		<strong>
    	<%= current_user.email %>
  		</strong>
		</div>
	  <div class="text-center">
  		<%= link_to 'logout', destroy_user_session_path, method: :delete %>
	  </div>
    </div>
  </div>
</div>

<%= yield %>

</body>
</html>
